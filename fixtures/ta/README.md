# Hand-calculated TA expectations

Stage 1A puts expected feature values here (boundaries, flat prices, equal extrema, session
gaps, rolls, missing data). The generator inputs in `contracts/` and `session_calendars/` stay
owned by the fixture pipeline.
