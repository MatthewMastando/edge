"""Detector implementations (Stage 1A).

Add one module per detector and register it with :class:`trading_core.ta.DetectorRegistry`.
The protocol in ``trading_core.ta.interfaces`` stays stable so the harness can import it
without taking this package's edits.
"""
